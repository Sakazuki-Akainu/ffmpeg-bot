services:
  - type: web
    name: tg-video-compress
    env: docker
    plan: free
    autoDeploy: true
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python3 wsgi.py"
    envVars:
      - key: PORT
        value: 8080
